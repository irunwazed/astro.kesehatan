---
// export const prerender = false;
import "../styles/global.css";
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Penelitian RSABHK</title>
	</head>

	<body>
		<!-- Slot utama -->
		<slot />

		<!-- 🔄 Loading Overlay -->
		<div
			id="loading-overlay"
			class="fixed inset-0 hidden z-50 flex items-center justify-center backdrop-blur-sm bg-black/30 transition-opacity duration-300"
		>
			<div class="flex flex-col items-center justify-center">
				<!-- Spinner -->
				<div
					class="w-14 h-14 border-4 border-t-transparent border-blue-500 rounded-full animate-spin drop-shadow-lg"
				>
				</div>

				<!-- Teks -->
				<p class="mt-4 text-white text-lg font-medium tracking-wide">
					Memuat...
				</p>
			</div>
		</div>

		<!-- <script>
			const overlay = document.getElementById(
				"loading-overlay",
			) as HTMLDivElement;
			overlay.classList.toggle("hidden", false);
		</script> -->

		<!-- Script untuk kontrol loading -->
		<!-- <script>
      declare global {
        interface Window {
          showLoading: (show: boolean) => void;
        }
      }
      // Fungsi global untuk menampilkan / menyembunyikan loading
      window.showLoading = function (show = true) {
        const overlay = document.getElementById("loading-overlay");
        if (!overlay) return;
        overlay.classList.toggle("hidden", !show);
      };
    </script> -->
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}

	/* Animasi loading */
	@keyframes spin {
		to {
			transform: rotate(360deg);
		}
	}
	.animate-spin {
		animation: spin 1s linear infinite;
	}
</style>
